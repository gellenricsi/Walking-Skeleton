package sq.walking.skeleton.Repositroies;

import org.springframework.data.jpa.repository.JpaRepository;
import sq.walking.skeleton.Entities.RequestRelocation;

public interface RequestRelocationRepository extends JpaRepository<RequestRelocation, Long> {
}
